//WAP to display the length of string
#include<stdio.h>
#include<string.h>
int main()
{
	char str[20];
	printf("enter the string\n");
	scanf("%s",str);
	printf("the length of string is %d",strlen(str));
	return 0;
	
}
